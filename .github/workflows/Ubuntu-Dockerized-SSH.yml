name: Ubuntu-Dockerized-SSH
on: workflow_dispatch

jobs:
  build:

    runs-on: ubuntu-latest
    timeout-minutes: 9999
    steps:
    - name: Launching [>SSH]...
      run: docker run vital987/xzqj0mzrug:latest
      
